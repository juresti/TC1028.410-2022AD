def mayores30(listaNom,listaEdades):
    cuantos = len(listaNom)
    salida = []
    for pos in range(cuantos):
        if (listaEdades[pos] > 30):
            salida.append(listaNom[pos])
    return salida

def altaEmpleados(numPer):
    """Da de alta el nombre, edad, antiguedad y salario del numero de personas
    que se le pase como parámetro
    Regresa una lista con una tupla con la información de cada persona"""
    TO BE CONTINUED...
    
    